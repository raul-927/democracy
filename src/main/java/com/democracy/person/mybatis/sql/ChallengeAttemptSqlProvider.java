package com.democracy.person.mybatis.sql;

import com.democracy.person.domain.ChallengeAttempt;
import org.apache.ibatis.jdbc.SQL;

public class ChallengeAttemptSqlProvider {

    public String insert(ChallengeAttempt challengeAttempt) {
        return new SQL() {
            {}
        }
            .toString();
    }

    public String update(ChallengeAttempt challengeAttempt) {
        return new SQL() {
            {}
        }
            .toString();
    }

    public String delete(ChallengeAttempt challengeAttempt) {
        return new SQL() {
            {}
        }
            .toString();
    }

    public String select(ChallengeAttempt challengeAttempt) {
        return new SQL() {
            {}
        }
            .toString();
    }
}
